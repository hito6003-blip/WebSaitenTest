services:
  - type: web
    name: exam-scoring-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
    plan: free # 💡 無料プランで動かす場合は free と指定
